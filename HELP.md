# Kirinuki 도움말

Kirinuki는 사용자가 확정한 CHZZK·YouTube·SOOP VOD 구간을 이 기기에 필요한
만큼만 준비해 편집하는 Linux용 로컬 앱입니다. 설치 뒤에는 앱 아이콘 또는
`kirinuki` 하나만 사용합니다. 내부 미디어·자막·브라우저 구성요소는 앱이 함께
시작하고 종료하므로 사용자가 별도로 연결하거나 관리할 항목이 없습니다.

## 처음 설치하기

현재 릴리스는 완전 독립형 AppImage가 아니라 Linux 소스 설치판입니다. 어떤
자막 방식을 고르더라도 다음 기본 도구가 먼저 설치되어 있어야 합니다.

- Node.js 22.13.0 이상과 npm
- Chromium 120 이상
- Python 3.11 이상
- FFmpeg와 ffprobe

Whisper 자막 방식을 선택할 때만 다음 빌드 도구도 설치되어 있어야 합니다.

- CMake
- tar
- C++ 컴파일러(예: `g++` 또는 `clang++`)

저장소의 `KirinukiHelper` 폴더에서 처음 한 번 실행합니다.

```bash
./setup.sh
```

기본 명령은 AudSeg로 설치합니다. 이 PC에서 로컬 Whisper 자막도 사용할 때는
위의 Whisper 전용 빌드 도구를 먼저 준비하고 다음 명령으로 설치합니다.

```bash
./setup.sh --mode whisper
```

소스 ZIP의 실행 권한이 빠졌다면 AudSeg는 `bash setup.sh`, Whisper는
`bash setup.sh --mode whisper`를 사용합니다. 설치는 시스템 패키지를 관리자
권한으로 대신 설치하지 않습니다. 빠진 도구가 있으면 안내한 뒤 멈추므로 해당
도구를 설치하고 같은 명령을 다시 실행하세요.

설치가 끝나면 앱 메뉴의 **Kirinuki** 또는 다음 명령으로 엽니다.

```bash
kirinuki
```

첫 실행과 업데이트 뒤 첫 실행에는 앱이 저장소 관리 구성요소를 자동으로 준비하고
버전을 맞춥니다. 별도 브라우저 확장 설치, 주소 입력, 서버 실행 단계는 없습니다.

## 공개 사이트에서 열기

`https://kirinuki.eff0rtchung.kr`은 소개·설치·앱 실행만 담당합니다. 실제 편집
프로젝트는 공개 웹페이지가 아니라 Kirinuki 전용 창에서 생성됩니다.

**Kirinuki에서 열기**를 누르면 브라우저가 등록된 앱을 열어도 되는지 확인할 수
있습니다. 허용하면 다음 형식의 앱 링크가 실행됩니다.

```text
kirinuki://open
kirinuki://open?source=<인코딩한 지원 VOD HTTPS URL>
```

`source`는 선택 사항이며 원본 입력칸만 채웁니다. 앱 링크는 `open`과 `source`
하나 외의 host·경로·매개변수, 중복 값, 계정 정보, 임의 포트, fragment를
거절합니다. 앱이 설치되지 않았거나 열기를 취소하면 공개 페이지의 설치 안내를
사용하세요. 실패한 웹 편집 화면으로 넘어가지는 않습니다.

## 새 프로젝트 시작

1. Kirinuki 앱을 엽니다.
2. 지원하는 VOD URL을 붙여넣습니다. `kirinuki open "VOD URL"`로 열었다면
   원본만 미리 채워집니다.
3. 원본을 확인하고 사용할 시작·끝을 `초`, `MM:SS` 또는 `HH:MM:SS`로 직접
   입력합니다.
4. **빈 구간 추가**로 컷을 더 만들고 필요한 순서로 정렬합니다.
5. 프로젝트 이름을 입력합니다.
6. 이번 작업의 권리·책임 확인란을 직접 읽고 선택합니다.
7. **편집기 열기**를 누릅니다.

공개 완료 VOD이면 앱이 각 선택 구간의 앞뒤 여유와 독립 디코딩에 필요한 최소
조각만 준비합니다. 전체 VOD를 무조건 내려받지 않습니다. 라이브·비공개·접근
제한·지원 밖 원본은 **내 파일 직접 연결**에서 사용 권한이 있는 로컬 파일을
선택하세요. 로그인, 쿠키, DRM, 지역 제한은 우회하지 않습니다.

## 지원 원본

- CHZZK 공개 다시보기
- `youtube.com/watch`, `youtu.be`, YouTube Shorts의 공개 완료 VOD
- `vod.sooplive.com/player/...`와 호환되는 공개 SOOP VOD

다른 사이트 안의 iframe, 진행 중인 라이브, 비공개·회원 전용·DRM 원본은 자동
준비 대상이 아닙니다. 공개 페이지를 볼 수 있다는 사실만으로 다운로드·편집·게시
권리가 생기지는 않습니다.

## 원본 확인과 편집 미디어

시작 화면의 원본 보기는 시각을 확인하기 위한 화면입니다. YouTube는 공식
privacy-enhanced IFrame API를 사용하고, CHZZK는 문서화되지 않은 VOD embed
주소를 만들지 않으며, SOOP은 지원되는 공식 VOD 화면을 사용합니다. frame 표시가
막혀도 **원본 새 탭**으로 직접 확인할 수 있습니다.

실제 편집, 자막 분석과 내보내기는 앱이 준비하고 검증한 로컬 미디어 또는
사용자가 직접 연결한 파일을 사용합니다. 컷 손잡이를 준비 범위 밖으로 옮기거나
**앞/뒤 더 받기**를 선택하면 필요한 방향의 누락분만 이어서 준비합니다. 새
미디어의 시각 매핑과 무결성이 모두 확인된 뒤 같은 재생 위치에서 교체되며,
준비가 실패하면 기존 편집 미디어를 유지합니다.

로컬 파일의 시작점과 원본 페이지 시각이 다르면 **페이지 시각 ↔ 로컬 원본
정렬**에서 오프셋을 먼저 맞춥니다. 앱이 자동 준비한 VOD는 원본 VOD 시각을
기준으로 고정하므로 별도 오프셋을 쓰지 않습니다.

## 컷 편집

사용자가 저장한 시작·끝은 확정 범위입니다.

- 자동 초벌은 컷 경계를 넓히거나 줄이지 않습니다.
- 컷을 자동으로 추가·삭제·병합하거나 순서를 바꾸지 않습니다.
- 사람이 손잡이, 시각 입력, 순서 버튼을 조작했을 때만 변경됩니다.
- 여러 컷을 선택해 위·아래 순서를 바꿀 수 있습니다.

영상 중간을 덜어낼 때는 재생헤드에서 `시작 [I]`, `끝 [O]`를 정하고 **구간
삭제**를 누릅니다. 삭제 뒤 연결된 이미지·음성·자막도 같은 시간축 규칙으로
이동합니다.

## 자막 만들기

### AudSeg

**AudSeg 이용하기**는 모델 없이 이 기기에서 소리 활동 구간을 찾아 빈 cue를
만듭니다. 가사나 대사를 사람이 직접 입력할 때 적합합니다. AudSeg의 분할 조건은
초벌 힌트일 뿐, 프로젝트 전체 자막에 4초 제한을 강제하지 않습니다.

### Whisper

글과 타이밍 초안이 모두 필요하면 **Whisper**를 선택합니다. Whisper 설치 명령을
사용한 앱은 설치 때 고른 모델과 이 기기의 엔진을 자동으로 준비합니다. API 키나
외부 자막 서비스가 필요하지 않고 분석 오디오를 외부 서버로 보내지 않습니다.

Whisper 결과도 정답으로 간주하지 않습니다. 고유명사, 노래, 겹말, 무음 경계와
1/60초 단위 타이밍은 원음을 들으며 사람이 검수해야 합니다.

### 에이전트로 자막 넣기

편집기 상단의 **에이전트로 자막 넣기**는 노래 가사의 타이밍을 자동으로
맞추도록 AI Agent에 전달할 프롬프트를 보여 줍니다. 사람이 완성한 컷 구도는
그대로 두고 자막 완성편집만 수행하도록 설계되어 있습니다. 엄격한 분석이
불가능한 경계도 보류하지 않고 가장 근거가 강한 프레임으로 확정해 반환하므로,
결과는 반드시 사람이 최종 검수합니다.

### 직접 검수

- 자막 시작·끝은 자유롭게 바꿀 수 있으며 일괄 4초 제한이 없습니다.
- 자막을 화면에서 직접 옮기고 크기·색상·검은 상자·레인을 바꿀 수 있습니다.
- 긴 문장은 읽기 쉬운 의미 단위로 나누고 필요하면 다른 레인을 추가합니다.
- 자막 속성 시트는 본문 대신 위치·크기·색상·상자 설정을 비교해 튀는 항목을
  찾는 데 사용합니다.
- 같은 컷의 자막과 이미지 에셋을 선택해 한쪽 구간에 정확히 맞출 수 있습니다.

## 이미지와 음성

PNG·WebP·JPEG 등 지원 이미지를 에셋으로 넣고 위치, 크기, 순서와 표시 구간을
조절할 수 있습니다. 웹에서 이미지 자체를 복사했다면 편집기에서 `Ctrl/Cmd+V`로
현재 시각에 붙여넣을 수 있으며 투명 영역을 보존합니다.

구간별 음성은 원본 음성과 독립된 레이어입니다. 시작·끝, 음량, 순서를 직접
조절합니다. 다른 프로젝트의 파일을 자동으로 재사용하거나 혼합하지 않습니다.

## 쇼츠 편집

본편의 **쇼츠 소스 만들기**에서 시간 구간과 원본 픽셀 영역을 고르면
1080×1920 작업공간을 만듭니다. 쇼츠는 본편과 별도 작업공간이며 본편 컷을
임의로 바꾸지 않습니다.

- 영상 레이어마다 crop, 배치, 크기, 불투명도, 표시 여부와 위·아래 순서를
  독립적으로 조절할 수 있습니다.
- 이미지·음성·자막은 자동 복사하지 않습니다.
- 필요한 원본 음성은 선택 영상에서 별도 음성 에셋으로 추가합니다.
- 같은 롱폼에서 여러 쇼츠를 만들 수 있으며 각 쇼츠는 이름과 작업공간으로
  구분됩니다.

## 단축키

한글 입력 중이거나 입력칸·버튼을 편집하는 동안 전역 단축키는 실행되지
않습니다. 삭제, 초기화, 내보내기, 장시간 작업 취소처럼 실수 비용이 큰 동작에는
단일 문자 단축키를 배정하지 않습니다.

- `Space`: 재생·일시정지
- `,` / `.`: 이전·다음 컷
- `J` / `K`: 같은 레인의 이전·다음 자막
- `A`: 현재 위치에 자막 추가
- `S`: 명시적으로 지금 저장
- `1`~`6`: 화면의 자막 색상 레지스터 적용 (`1`은 흰색)

버튼 툴팁에 표시된 현재 화면의 단축키를 기준으로 사용하세요.

## 저장과 새 작업

저장하지 않은 프로젝트는 Kirinuki 창을 닫으면 폐기하는 것이 기본입니다.
서버 세션이나 로그인 계정에 자동 보관되지 않습니다.

- 이어서 편집하려면 닫기 전에 **지금 저장**을 누릅니다.
- **저장 목록**에서 저장본을 명시적으로 골라야 다시 열립니다.
- 새 프로젝트를 시작하면 이전 프로젝트의 영상·캐시·편집 상태를 자동 연결하지
  않습니다.
- 비정상 종료 복구본이 있더라도 선택하기 전에는 현재 프로젝트로 적용하지
  않습니다.
- **로컬 자료 관리**에서 저장본과 앱이 준비한 VOD 캐시를 프로젝트별로 확인하고
  삭제할 수 있습니다.

내보낸 파일이나 사용자가 직접 고른 원본은 Kirinuki가 임의로 지우지 않습니다.
내보내기와 sidecar 무결성을 확인한 뒤 **세션 완료·로컬 재료 삭제**를 직접
선택한 경우에만 해당 프로젝트가 단독 소유한 준비 자료를 정리합니다.

## 내보내기

**영상 내보내기**에서 결과 제목, 저장 폴더와 지원 형식을 정합니다. 영상,
`.kirinuki-session.json`, 자막이 있을 때 SRT를 같은 폴더에 저장하고 크기와
SHA-256을 검증합니다. 일반 다운로드 폴백이나 검증 실패는 완료로 간주하지
않으며 로컬 재료도 삭제하지 않습니다.

긴 영상, 4K 원본, 많은 레이어는 메모리와 GPU 사용량이 큽니다. 다른 무거운 탭을
닫고 더 짧은 구간으로 나누어 출력하면 안정성이 좋아질 수 있습니다.

## 개인정보와 세션

Kirinuki에는 로그인, 계정, telemetry, analytics, 사용기록 수집이 없습니다.
원본 URL, 편집 내용, 저장본, 캐시와 내보낸 파일은 사용자가 명시한 이 기기의
저장소에만 둡니다. 앱은 다음 접속자를 알아보기 위한 세션 식별자를 보존하지
않습니다.

플랫폼 원본을 여는 브라우저 요청과 설치 시 검증된 artifact 다운로드는 필요한
네트워크 동작입니다. 각 플랫폼이 자체적으로 처리하는 쿠키·접속 기록은 해당
서비스 정책에 따르며 Kirinuki가 수집하는 기록이 아닙니다.

## 문제 해결

### 앱 아이콘을 눌러도 열리지 않음

터미널에서 `kirinuki`를 한 번 실행해 사람이 읽을 수 있는 진단을 확인합니다.
명령도 없으면 저장소에서 `bash setup.sh`를 다시 실행하세요. 시스템 의존성이
빠졌다는 안내가 나오면 해당 도구를 설치한 뒤 다시 실행합니다.

### 공개 사이트의 앱 열기가 반응하지 않음

Kirinuki가 설치되어 있는지 확인하고 브라우저의 외부 앱 열기 질문을 허용하세요.
그래도 열리지 않으면 앱 아이콘이나 `kirinuki`로 직접 실행합니다. 공개 사이트
자체에서 편집을 계속하려고 시도하지 마세요.

### VOD 편집 영상을 준비하지 못함

먼저 원본이 공개 완료 VOD인지, URL이 지원 형식인지 확인합니다. 앱을 완전히
닫았다가 다시 열고 **편집 영상 다시 준비**를 한 번 누릅니다. 계속 실패하면
플랫폼 변경 또는 시스템 FFmpeg/Python 문제일 수 있으므로 사용 권한이 있는
로컬 파일을 직접 연결합니다.

### 새 프로젝트에 이전 영상이 보임

현재 앱 창을 닫고 Kirinuki를 다시 연 뒤 새 프로젝트를 시작합니다. 저장
목록이나 복구본을 선택하지 않았는데도 이전 영상이 연결되면 새 작업을 진행하지
말고 진단 정보를 첨부해 문의하세요. **로컬 자료 관리**에서 오래된 준비 자료를
지울 수 있지만 사용자가 직접 고른 원본 파일은 그 화면에서 삭제되지 않습니다.

### Whisper가 준비되지 않음

설치에서 Whisper 방식을 골랐는지 확인한 뒤 앱을 재실행합니다. 첫 준비는 모델
다운로드와 로컬 빌드 때문에 오래 걸릴 수 있습니다. 개발자·관리자가 원인을
확인할 때만 `kirinuki doctor`를 사용합니다.

### 저장한 프로젝트가 원본을 다시 요구함

브라우저 보안상 사용자가 직접 고른 로컬 파일 권한은 다시 선택해야 할 수
있습니다. 자동 준비한 VOD 캐시가 사용자가 삭제했거나 세션 완료로 정리됐다면
앱이 필요한 구간을 다시 준비합니다.

## 개발자·관리자 진단

아래 명령은 일반 작업 절차가 아니라 장애 진단용 폴백입니다.

```bash
kirinuki doctor
kirinuki status
kirinuki stop
```

저수준 npm 명령과 내부 구성요소 이름·주소는 개발용 구현 세부사항입니다.
일반 사용자에게 이를 직접 시작하거나 연결하도록 안내하지 마세요.

문의: **lostfragment@naver.com**
