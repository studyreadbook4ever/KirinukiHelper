# 치지직·YouTube·SOOP 키리누키 스튜디오 도움말

Kirinuki는 CHZZK·YouTube·SOOP VOD의 URL과 사용자가 직접 입력한 구간을
`http://127.0.0.1:4320`의 로컬 편집기로 옮겨 컷, 이미지 에셋, 음성, 한국어
자막을 다듬고 영상으로 내보냅니다. 공개 완료 VOD는 선택한 범위의 최초 앞뒤
10초와 디코딩에 필요한 최소 조각만 이 PC에 자동 준비하고, 사람이 더 넓힌
방향은 누락 구간만 추가 준비할 수 있습니다. 그 밖의 소스는 권한 있는 로컬
원본을 연결합니다. 로그인·쿠키·DRM·접근 제한은 우회하지 않습니다.

## 빠른 시작

Linux에서는 저장소의 `KirinukiHelper` 폴더에서 처음 한 번:

```bash
./setup.sh
```

GitHub 소스 ZIP에서 실행 권한이 빠졌다면 처음에는 `bash setup.sh`를
사용하세요. 성공한 설정이 실행 권한을 복원합니다. 자동 실행은 Chromium 120
이상용이며 과거 전체 확장판의 수동 설치나 개발자 모드는 필요하지 않습니다.

그다음 작업부터는:

```bash
kirinuki start "치지직·YouTube·SOOP VOD URL"
./kirinuki.sh start "치지직·YouTube·SOOP VOD URL"
```

일반 `chromium` 명령은 Kirinuki 전용 프로필을 열거나 로컬 서버를 시작하지
않습니다. `kirinuki start`는 고정 `127.0.0.1:4320` 스튜디오와
`127.0.0.1:4319` VOD/caption gateway를 준비한 뒤 전용 프로필로 시작 화면을
엽니다. Whisper를 선택했을 때만 gateway 뒤의 private `127.0.0.1:4318`
엔진이 추가로 필요합니다. 런타임 빌드가 맞지 않으면 `kirinuki stop` 후
`kirinuki start`로 관리형 서버를 다시 시작하세요.

이 실행은 UI·저장소·다른 탭 접근 권한이 없는 최소 `streaming-companion/`만
전용 Chromium에 로드합니다. 현재 protocol marker가 없는 구 실행이나 marker가
다른 실행은 재사용하지 않습니다. `linux-helper`가 과거 전체 확장판의 관리
프로세스를 프로필·PID·시작 시각·실행 파일·명령행까지 정확히 재검증한 경우에만
한 번 정상 종료를 요청하고 최소 companion으로 다시 엽니다. 식별할 수 없는
프로세스는 종료하지 않습니다.

URL 없이 `./kirinuki.sh`를 실행하면 한글 메뉴에서 설치·실행·진단·상태·종료를
고를 수 있습니다. 도우미는 기존 Chromium 프로필을 건드리지 않는 전용
프로필을 사용하고, 빠진 시스템 도구를 대신 설치하거나 `sudo`를 자동 실행하지
않습니다.

이전 전체 브라우저 확장판이 이미 설치된 전용 프로필에만 프로젝트가 남아 있다면
그 설치본의 **저장소 한 번 옮기기**를 먼저 사용하세요. 현재 릴리스는 과거
bundle을 다시 제공하지 않고 localhost IndexedDB로 옮기는 단발성 호환 창구만
유지합니다. 이동하는 한 번에만 studio server를 종료하고
`node --import tsx scripts/local-studio-server.ts start --enable-legacy-migration`
으로 시작하며, 완료 뒤에는 옵션 없이 다시 시작합니다. 이 endpoint는 기본 실행과
공개 배포에서는 닫혀 있습니다.

1. `http://127.0.0.1:4320` 시작 화면에 CHZZK·YouTube·SOOP VOD URL을
   붙여넣습니다. `kirinuki start URL`로 열었다면 URL은 미리 채워집니다.
2. 원본을 별도 탭에서 확인하고 사용할 시작·끝을 `초`, `MM:SS` 또는
   `HH:MM:SS`로 직접 입력합니다. **빈 구간 추가**로 필요한 컷을 반복합니다.
3. 프로젝트 이름을 입력하고 이번 사용의 권리·책임 확인란 여섯 개를 직접
   선택합니다.
4. **편집기 열기**를 누릅니다. Kirinuki는 다른 열린 영상 탭의 플레이어를
   읽거나 제어하지 않습니다.
5. 공개 완료 CHZZK·YouTube·SOOP VOD면 정책 확인 뒤 최초 편집 영상 준비가 자동으로 시작됩니다. 실패했거나 다시 확인할 때만 **편집 영상 준비/다시 준비**를 누릅니다. 라이브·비공개·접근 제한 원본이면 **내 파일 직접 연결**에서 권한 있는 같은 영상의 로컬 파일을 선택합니다.
6. 기본 **AudSeg 이용하기**로 빈 타이밍을 만듭니다. 글 초안도 필요하면 오른쪽 **Whisper** → **이 PC의 Whisper 자동 연결**을 누릅니다. 편집기가 loopback 도우미와 실행 모델을 자동으로 확인하므로 파일이나 경로를 고르지 않습니다.
7. **활성 컷 전체 초벌 만들기**를 누릅니다.
8. 자막과 컷을 원음에 맞춰 검수한 뒤 **영상 내보내기**를 누릅니다.

자막 내용을 직접 쓰되 소리가 있는 구간의 타이밍 틀만 빨리 받고 싶으면 AudSeg를 쓸 수 있습니다. 글까지 자동으로 받아 적고 싶을 때만 Whisper를 연결합니다. 두 방식 모두 API 키가 필요 없고 인터넷 자막 서비스로 음성을 보내지 않습니다.

## 안전한 키보드 단축키

시작 화면과 편집기의 버튼 위에 마우스를 올리면 현재 화면에서 쓸 수 있는
키보드 단축키가 표시됩니다. 한글 입력 중이거나 입력칸·버튼을 편집하는 동안에는
실행되지 않습니다. 삭제, 전체 초기화, 복구본 덮어쓰기, 내보내기, 장시간 자막
생성, 진행 작업 취소처럼 실수 비용이 큰 동작에는 A–Z 단축키를 배정하지
않았습니다.

시작 화면은 URL과 시간 입력을 명시적인 버튼으로 확정합니다. 편집기에서는
`Space`로 재생·일시정지, Space 가까이의 `,`/`.`로 이전·다음 컷,
`J`/`K`로 같은 자막 레인의 이전·다음 자막, `S`로 로컬 임시저장,
`A`로 현재 위치 자막 추가를 할 수 있습니다. 선택 자막 색은 화면에 표시된
레지스터 순서대로 `1`~`6`을 누르며, `1`은 항상 흰색입니다.
전체 매핑은 각 버튼의 툴팁을 기준으로 합니다.

## 지원 소스

- 치지직 다시보기
- `youtube.com/watch`
- `youtu.be`
- YouTube Shorts
- 최상위 탭으로 연 YouTube embed
- `vod.sooplive.com/player/...` SOOP VOD
- 호환용 레거시 SOOP/AfreecaTV VOD 재생 주소

localhost 시작 화면의 자동 준비는 공개 완료 VOD만 지원합니다. 진행 중인
CHZZK·YouTube 라이브와 다른 사이트 안에 삽입된 iframe은 지원하지 않습니다.
라이브나 접근 제한 원본은 사용 권한이 있는 로컬 파일을 직접 연결하세요.

## 원본 스트리밍 창

시작 화면의 **원본 스트리밍 창**은 구간의 시작·끝 시각을 고르고 다시 확인하는
client-side viewer입니다.

- YouTube: 공식 `https://www.youtube-nocookie.com/embed/<video-id>`
- SOOP: 공식 `https://vod.sooplive.com/player/<vod-id>/embed`
- CHZZK: 문서화되지 않은 VOD embed 주소를 만들지 않고 canonical
  `https://chzzk.naver.com/video/<video-id>` 페이지를 그대로 표시

CHZZK가 frame 표시를 막거나 어떤 플랫폼 플레이어가 열리지 않아도 URL 입력 옆의
**원본 새 탭**은 항상 사용할 수 있습니다. viewer 요청은 브라우저가 플랫폼으로
직접 보내며 Kirinuki 서버는 스트림을 proxy하거나 브라우저 쿠키·로그인 token을
받지 않습니다. 이 화면은 시간 선택용입니다. 실제 편집·자막 오디오·내보내기는
별도로 로컬 준비·검증된 compact MP4만 사용합니다.

CSP `frame-src`는 실제 frame이 사용하는 `chzzk.naver.com`,
`www.youtube-nocookie.com`, `vod.sooplive.com`의 세 HTTPS Origin만 허용합니다. 라이브·clip·playlist,
지원하지 않는 경로, `youtube.com.evil.example` 같은 스푸핑 host는 frame을 만들지
않고 fail-closed합니다.

## 로컬 편집 영상 준비

CHZZK·YouTube·SOOP 공개 VOD는 새 편집기 진입 시 전체 원본 대신 최초 편집 영상을 자동으로 준비합니다. 주소만 보고 플랫폼 방식을 선택한 뒤 활성 사용자 선택 구간을 합치고 각 범위의 최초 `±10초`만 이 PC에 준비하며, **편집 영상 준비/다시 준비**는 자동 준비 실패나 명시적인 재확인에 사용합니다. 컷의 **앞 30초·뒤 30초 더 받기**를 누르거나 손잡이를 준비 범위 밖으로 옮기면 clip별 기존 범위를 줄이지 않고 필요한 방향의 누락분만 이어 받습니다.

- CHZZK는 선택된 공개 HLS rendition의 누적 `EXTINF`를 플레이어 시간축으로 증명한 뒤, 그 시간축에 걸치는 fMP4 조각만 받아 로컬에서 정확히 자릅니다. 원격 fMP4의 원시 PTS를 페이지 시각으로 오인하지 않으며, 디코딩용 조각 경계는 편집 가능한 10초 핸들을 늘리지 않습니다.
- YouTube는 단일 공개 완료 VOD만 처리합니다. 로컬 companion이 자격증명 없는 `yt-dlp`로 정확한 video/audio 포맷과 0초 원점을 확인한 뒤 FFmpeg로 필요한 범위를 로컬 취득·검증·병합합니다.
- SOOP은 여러 VOD 파트를 한 연속 원본 시간축으로 계산하고, 선택 범위가 걸친 파트의 필요한 부분만 준비합니다.

추가 준비 중에도 현재 로컬 영상과 편집 상태를 계속 보존합니다. 새 조각·원본 버전·MP4·시간 매핑의 검증과 저장이 모두 끝난 뒤에만 같은 재생 위치에서 새 영상으로 바꾸며, 취소하거나 실패하면 기존 결과로 돌아갑니다. 본편과 쇼츠가 같은 원본 clip을 참조하면 두 작업공간의 필요 범위를 합쳐 다른 컷의 준비 범위를 줄이지 않습니다.

브라우저가 `127.0.0.1`의 미디어 주소를 읽는 것은 원격 스트리밍이나 CDN 중계가 아닙니다. 준비와 무결성 검증이 끝난 compact MP4를 같은 PC의 companion이 전달하는 로컬 파일 전송입니다. 편집·미리보기·자막 오디오·내보내기는 원본 VOD 시각↔compact 시각 매핑만 사용하고 원격 재생으로 폴백하지 않습니다.

일반 편집 흐름은 PATH의 임의 `yt-dlp`나 사용자 yt-dlp 설정을 실행하지 않습니다. `./kirinuki.sh setup`이 공식 고정 버전을 크기와 SHA-256으로 검증해 사용자별 XDG 경로에 설치하고, 확인한 Node·Python·FFmpeg·ffprobe의 절대 경로만 companion에 전달합니다. Node 22+, Python 3.11+, FFmpeg와 ffprobe는 시스템에 있어야 하며 도우미가 관리자 권한이나 패키지 설치를 대신 수행하지 않습니다. 도구 또는 필요한 H.264/AAC/MP4 기능이 없거나 영상이 공개 완료 VOD가 아니면 이유를 표시하고 중단합니다. 쿠키·로그인·netrc·DRM·지역 또는 접근 제한 우회는 사용하지 않습니다. 다운로드 가능 여부, 플랫폼 약관, 콘텐츠 권리와 게시 정책은 사용자가 확인해야 합니다.

VOD runtime만 따로 진단하려면 다음 명령을 사용할 수 있습니다.

```bash
npm run vod-runtime:doctor
npm run vod-runtime:setup
npm run vod-runtime:status
npm run vod-runtime:stop
```

## 매 작업 권리·책임 확인

새 편집기, **계속 편집**, **복구본 선택**은 실행할 때마다
이번 원본 회차와 목적에 맞는 권리·책임 확인을 새로 요구합니다. 과거 작업의
입력을 자동으로 채우거나 방송인별 정책 목록·정책 본문·판정 결과를 내장하지
않습니다.

대상 VOD의 키리누키 허용, 별도 서면 허락 또는 공식 편집 권한 중 실제로
해당하는 근거가 있는지는 앱 밖에서 확인합니다. Kirinuki에는 증빙 파일이나
참조명을 입력하지 않습니다. 화면의 여섯 확인란을 모두 직접 선택하면 양식
검증 후 통과합니다.

통과 여부는 필수 값과 형식만 검사합니다. Kirinuki는 네트워크에 접속해 실제
허락·증빙의 진실성을 판정하지 않습니다. 같은 프로젝트·원본·목적의 열린 편집기를
시간만으로 자동 잠그지 않습니다. 양식 오류와 런타임·정책 세션 오류는 서로 다른
문구로 표시됩니다.

사용자 확인은 실제 권리나 게시 허가를 새로 만들지 않으며 Kirinuki의 법률·정책
승인을 뜻하지 않습니다. 편집기에서는 전체 입력을 프로젝트나 복구본에 복제하지
않고 현재 localhost 탭의 `sessionStorage`에 대상 고정 게이트에 필요한 최소
정보만 둡니다.
실제 증빙 원문은 Kirinuki에 넣지 마세요.

## 사용자 확정 컷

사용자가 저장한 시작·끝은 확정 범위입니다.

- 자동 초벌은 재미있는 구간을 새로 고르지 않습니다.
- 경계를 자동으로 넓히거나 줄이지 않습니다.
- 컷 순서를 자동으로 바꾸지 않습니다.
- 겹치는 컷도 임의로 합치거나 버리지 않습니다.
- 편집기에서 사람이 손잡이를 끌거나 값을 입력했을 때만 경계가 바뀝니다.

여러 컷을 체크한 뒤 `Alt+↑`, `Alt+↓` 또는 그룹 이동 버튼으로 순서를 바꿀 수 있습니다.

### 영상 중간 구간 삭제

컷의 양끝만 줄이는 것이 아니라 타임라인 중간을 덜어내려면:

1. 삭제 시작점에 재생헤드를 둡니다.
2. **시작 I**를 누릅니다.
3. 삭제 끝점으로 이동합니다.
4. **끝 O**를 누릅니다.
5. 선택 범위를 확인하고 **구간 삭제**를 누릅니다.

삭제 뒤 뒤쪽 영상, 이미지, 음성 설정, 자막은 같은 시간만큼 앞으로 이동합니다. 삭제 구간과 겹친 항목은 남는 영상 구간에 맞춰 잘리거나 분할됩니다. 큰 삭제 전에는 **지금 저장**을 권장합니다.

## 페이지 시각과 로컬 원본 맞추기

같은 방송이라도 페이지 시각과 로컬 파일의 첫 프레임 시각이 다를 수 있습니다.

1. 페이지에서 쉽게 구분되는 말이나 장면의 시각을 확인합니다.
2. 로컬 원본에서 같은 지점을 찾습니다.
3. `로컬 원본 시각 - 페이지 시각`을 **페이지 시각 ↔ 로컬 원본 정렬**에 초 단위로 입력합니다.
4. **적용**을 누르고 컷 시작점을 몇 곳 재생해 확인합니다.

예를 들어 페이지 10.000초의 장면이 로컬 파일 12.350초에 있다면 오프셋은 `2.350`입니다.

## 자막 초벌 방식

편집기에는 정확히 두 가지 방식이 있으며, 새 환경의 기본은 AudSeg입니다.

### AudSeg: 모델 없는 빈 타이밍

AudSeg는 음성을 글로 바꾸지 않습니다. 브라우저에서 오디오 활동 구간을 찾고 각 구간에 비어 있는 편집용 cue를 만듭니다.

- 모델 다운로드와 companion 실행이 필요 없습니다.
- 20ms RMS 프레임, 적응형 소음 바닥, 히스테리시스, debounce·padding·merge를 사용합니다.
- AudSeg가 처음 만드는 빈 타이밍 cue에서만 긴 활동 구간을 조용한 골짜기 우선으로 최대 4초 단위로 나눕니다.
- 결과는 같은 입력에서 재현 가능하며 네트워크 요청을 하지 않습니다.
- 음악, 효과음, 박수, 키보드 소리도 활동으로 감지될 수 있습니다.
- 화자 구분과 자막 텍스트는 제공하지 않습니다.

AudSeg는 “정확도가 낮은 STT”가 아니라 “수동 자막용 타이밍 스캐폴드”입니다. 모든 cue를 원음과 대조하고 텍스트를 직접 채워야 합니다.

### 로컬 Whisper: 글과 타이밍

Whisper는 선택 컷의 16kHz mono PCM을 이 기기의 loopback companion으로 보내 한국어 timed transcript를 만듭니다.

- 실제 segment와 word timestamp를 타이밍 권위로 사용합니다.
- 문장 길이만으로 시간을 새로 추정하지 않습니다.
- 알아듣기 어렵거나 anchor coverage가 낮은 cue는 검수 대상으로 남깁니다.
- 인터넷 API, 계정, API 키가 필요 없습니다.

최초 한 번:

```bash
npm run caption-stack:doctor
npm run caption-stack:setup
npm run caption-stack:start
```

다음 작업부터는 `npm run caption-stack:start`를 실행합니다. 편집기에서 연결하기:

1. AudSeg 오른쪽 **Whisper**를 누릅니다. 이 단계는 연결 패널만 엽니다.
2. **이 PC의 Whisper 자동 연결** 또는 **자막 초안 만들기**를 누릅니다.
3. 파일 선택 없이 **연결됨**과 현재 실행 모델 이름이 표시되는지 확인합니다.
4. 자막 초안을 만듭니다.

Kirinuki는 exact `http://127.0.0.1:4320` Origin에서 gateway `http://127.0.0.1:4319`에 직접 pair·probe하고 실제 companion 상태와 모델을 검증합니다. 선택적인 whisper.cpp 엔진은 gateway 뒤의 private `127.0.0.1:4318`에서만 동작합니다. 파일 경로·핸들·token은 프로젝트나 브라우저 저장소에 저장하지 않습니다.

지원 profile과 실제 모델:

| profile | 모델 | 용도 | 크기 |
|---|---|---|---:|
| `draft` | `tiny-q5_1` | 빠른 초안 | 약 32 MB |
| `light` | `base-q5_1` | 저사양 PC | 약 60 MB |
| `auto` | `small-q5_1` | 속도·품질 균형 | 약 190 MB |
| `quality` | `medium-q5_0` | 정확도 우선 | 약 539 MB |

`auto`는 PC 메모리가 6GiB 미만이면 `light` / `base-q5_1`로 하향될 수 있습니다. 편집기의 모델 목록은 설치·교체 버튼이 아니라 연결된 실제 모델을 자동 표시하는 안내입니다. 모델을 바꾸려면 실행 중인 stack을 정상 종료한 뒤 다시 setup·start하고 자동 연결을 다시 누르세요.

상태와 종료:

```bash
npm run caption-stack:status
npm run caption-stack:stop
```

저사양 기기에서는 `draft`를 유지하고 Whisper 활성 컷을 16개 이하의 작은 묶음으로 나누는 편이 안전합니다.

### 어떤 방식을 고를까

| 목적 | 선택 |
|---|---|
| 한국어 글 초안과 실제 STT 타임스탬프가 필요함 | 로컬 Whisper |
| 글은 직접 쓰고 소리 구간만 빠르게 펼치고 싶음 | AudSeg |
| companion을 설치할 수 없는 환경 | AudSeg |
| 음악·효과음이 많고 발화만 구분해야 함 | 로컬 Whisper 후 사람 검수 |

방식을 바꾸면 체크포인트 지문도 달라집니다. 다른 방식의 완료 컷을 같은 결과로 오인해 건너뛰지 않습니다.

## 초벌 실행과 재개

Whisper는 한 번에 활성 컷 최대 16개이며, AudSeg는 프로젝트의 모든 활성 컷을 한 컷씩 순차 처리합니다. 두 방식 모두 한 번의 실행에서 새 초벌 cue는 최대 10,000개입니다. 실행 전 컷 수, 총 길이와 선택 방식을 확인합니다.

- Whisper를 고르면 companion 연결과 실제 모델을 확인한 뒤 오디오를 추출합니다.
- AudSeg를 고르면 companion 주소·세션 상태와 무관하게 브라우저에서 바로 분석합니다.
- AudSeg는 컷 하나당 30분·16kHz mono Float32 PCM 128MiB 안전 상한을 적용합니다. 상한을 넘는 긴 구간은 먼저 여러 컷으로 나눠 주세요.
- 취소하면 아직 시작하지 않은 컷은 처리하지 않습니다.
- 컷 하나가 끝날 때마다 프로젝트와 체크포인트를 저장합니다. AudSeg는 16개를 넘는 프로젝트에서도 완료한 모든 컷의 체크포인트를 보존합니다.
- 오류나 탭 종료 뒤 같은 범위·방식·pipeline 지문으로 다시 실행하면 완료 컷을 건너뜁니다.
- 새 전체 실행, 다른 원본 연결, 컷 범위 변경, 방식 변경은 낡은 체크포인트를 재사용하지 않습니다.

사람이 직접 만든 자막과 사람이 수정한 AI 자막은 재실행으로 덮어쓰지 않습니다.

## 자막 초안 규칙

Whisper 자동 본문 cue와 AudSeg 자리표시자 cue의 기본 화면 위치는 아래 중앙입니다.

- `x=0.5`, `y=0.84`
- 배경 없는 한 줄
- 기본 글꼴 Pretendard ExtraBold
- AudSeg가 처음 만드는 빈 타이밍 cue는 최대 4초 단위로 나뉨
- 가능한 표시 시간 0.65초 이상
- 한국어 폭 20 단위 이하
- 목표 읽기 속도 초당 16폭 단위 이하
- 문장 끝 `.` 제거
- `?`, `!`, `…`, `~` 유지
- 기본 화자 흰색
- 식별 가능한 다른 화자는 결정적인 고유 색

**AI 자막 전체를 기본 위치로 정렬**은 AI origin 자막의 위치만 아래 중앙으로 맞춥니다. 텍스트, 시각, 레인, 색과 직접 만든 자막은 유지합니다. 평소 로딩과 재실행은 사람이 옮긴 자막 위치를 자동으로 되돌리지 않습니다.

편집기 자막 전체에는 4초 최대 길이 제한이 없습니다. 최대 4초는 AudSeg가 처음 만드는 빈 타이밍 cue에만 적용되는 생성 조건입니다. Whisper 초벌을 포함해 만들어진 모든 자막의 시작과 끝은 발화와 연출에 맞춰 자유롭게 옮기거나 늘릴 수 있으며, 저장하거나 초벌을 다시 실행해도 사람이 조정한 길이를 4초로 되돌리지 않습니다.

## 자막 직접 검수

자막 글, 색, 크기와 위치는 입력하는 즉시 영상 미리보기와 타임라인에 반영되고 CURRENT 자동 저장이 시작됩니다. 입력칸에서 포커스를 빼거나 편집기 페이지를 새로고침할 필요가 없습니다.

1. 각 cue를 재생하며 원음을 듣습니다.
2. 고유명사, 빠른 발화, 감탄사, 겹친 목소리를 확인합니다.
3. 노란 **검수 필요** 표시가 있으면 반드시 텍스트와 시간을 확인합니다.
4. 타임라인 블록 양끝 손잡이 또는 시작·끝 입력으로 시간을 조절합니다.
5. 화면 자막을 직접 끌거나 가로·세로 위치 값을 입력해 배치합니다.
6. 화자별 색을 정합니다.

자막 레인은 기본 2개입니다. 같은 시각에 여러 자막을 올릴 수 있고 `+`로 레인을 늘릴 수 있습니다. 자막 트랙을 우클릭하면 현재 위치에 추가하고, 자막 블록을 우클릭하면 삭제할 수 있습니다.

색상 선택기 오른쪽 레지스터는 고정 흰색 `#FFFFFF`와 최근 사용한 비흰색 5개를 기억합니다. 레지스터 색을 누르거나 표시된 숫자 `1`~`6`을 누르면 선택 자막에 즉시 적용되며, `1`은 항상 흰색입니다. 선택 자막 패널의 `← 왼쪽 자막 · J`와 `오른쪽 자막 · K →`는 같은 레인의 실제 표시 자막만 시간순으로 오가고 컷 경계를 넘을 수 있습니다. 첫 자막과 마지막 자막에서는 해당 방향 버튼이 비활성화되며 끝에서 반대편으로 순환하지 않습니다.

## 이미지 에셋

웹에서 이미지 자체를 복사한 뒤 편집기에서 `Ctrl/Cmd+V`를 누르면 현재 재생 위치에 들어갑니다. **클립보드 이미지 붙여넣기**와 파일 선택도 사용할 수 있습니다.

- PNG와 WebP의 투명 배경을 보존합니다.
- 위치, 크기, 불투명도와 표시 시간을 조절할 수 있습니다.
- 타임라인 블록 양끝을 끌어 시작·끝을 바꿀 수 있습니다.
- 붙여넣은 Blob은 프로젝트와 함께 IndexedDB에 저장됩니다.

### 자막과 에셋 시각 맞추기

타임라인 **자석**은 기본으로 켜져 있습니다. 자막 또는 에셋 경계를 가까이 끌면 같은 컷의 반대 종류 경계에 붙고 가이드가 나타납니다. 드래그 중 `Alt`를 누르면 그 드래그에서만 자석을 잠시 끕니다.

양끝을 완전히 같게 만들려면 같은 컷의 자막과 에셋을 차례로 선택한 뒤:

- 자막 패널의 **선택 에셋 구간에 정확히 맞춤**
- 에셋 패널의 **선택 자막 구간에 정확히 맞춤**

중 하나를 누릅니다.

## 구간별 음성

음성 트랙은 고정 1개입니다. 현재 위치에서 **+ 현재 위치 음성 설정**을 누르거나 음성 트랙을 우클릭해 설정 구간을 만듭니다.

- 구간 음량
- 완전 음소거
- 페이드 인
- 페이드 아웃
- 시작·끝

을 조절할 수 있습니다. 매우 짧은 단어 음소거는 파형만 믿지 말고 반복 재생해 앞뒤 음소까지 직접 확인하세요.

## 로컬 임시저장과 복구

- **지금 저장**: 즉시 저장본 생성
- 5분마다 자동 임시저장
- 프로젝트별 최근 5개 유지
- 복구본을 불러오기 직전 현재 상태를 `복원 직전`으로 자동 저장
- 시작 화면의 **저장된 편집 → 계속 편집**을 누르고 이번 사용의 권리·책임 확인 뒤 현재본 열기. 저장된 공개 원본 링크는 별도 탭으로 다시 열 수 있지만 편집 재개는 정확한 저장 프로젝트를 기준으로 함
- **복구본 선택**을 누르고 이번 1회 권리·책임 확인 뒤 최근 5개 목록 열기

프로젝트, 자막, 이미지 Blob, 파일 핸들, 임시저장은 전용 Chrome/Chromium
프로필의 `http://127.0.0.1:4320` IndexedDB에 있습니다. 이 사이트 데이터를
지우거나 다른 포트·프로필로 열면 보이지 않으므로 중요한 작업은 프로젝트 JSON도
내보내세요. 이전 브라우저 확장 Origin의 IndexedDB에 데이터가 남아 있다면
단발성 마이그레이션으로 프로젝트·복구본·이미지 에셋을 한 번 옮긴 뒤, 이미
설치된 과거 bundle을 정상 실행 경로로 계속 사용하지 않습니다. 파일 핸들은
필요할 때 다시 선택할 수 있습니다.

자동 준비한 CHZZK·YouTube·SOOP VOD 조각과 로컬 MP4는 Linux 기본값으로 `${XDG_STATE_HOME:-~/.local/state}/kirinuki-vod-runtime/vod-fragments`에 별도 보관됩니다. 이미 준비가 끝난 프로젝트는 이 파일과 무결성 영수증을 먼저 열어 원격 VOD를 다시 읽지 않고 이어서 편집합니다. 브라우저의 **모든 로컬 작업 초기화**는 이 폴더를 지우지 않습니다. 비우려면 `./kirinuki.sh stop`으로 companion을 먼저 정상 종료하고 파일 관리자에서 정확한 `vod-fragments` 폴더만 삭제하세요.

## 쇼츠 편집기

편집기 상단의 **쇼츠 편집기**는 본편과 분리된 1080×1920 작업 공간을 엽니다. 아직 보낸 컷이 없어도 빈 캔버스가 열리고 저장됩니다.

한 본편에서 여러 쇼츠를 만들 때는 쇼츠 화면의 **쇼츠 작업** 선택기를 사용합니다.
**+ 새 작업**은 빈 쇼츠를 만들고, **복제**는 현재 쇼츠의 영상·자막·배치를 새
작업으로 복사합니다. 작업 이름을 먼저 정해 두면 선택기와 저장 상태에서 어떤
쇼츠인지 바로 구분할 수 있습니다. 각 작업의 영상·자막·실행 취소/다시 실행·
내보내기 상태는 서로 섞이지 않으며, 작업을 전환해도 본편 컷은 바뀌지 않습니다.
삭제는 현재 선택한 쇼츠 하나만 확인 후 지우고 마지막 남은 쇼츠는 삭제하지 않습니다.

1. 본편에서 **쇼츠 소스 만들기**를 누르고 타임라인의 시작·끝을 정합니다. 기본값은 현재 재생 위치부터 최대 5초입니다. **이 컷의 끝까지 가져오기**는 이미 정한 시작점은 보존하고 그 시작점이 속한 컷의 끝만 맞춥니다. **현재 컷 전체**, 시작·끝별 **현재 위치**·**컷 시작/끝**, ±0.1초·±1초 버튼과 시간 입력을 함께 쓸 수 있습니다. 시간 입력의 ↑/↓는 0.1초, Shift+↑/↓는 1초이며 **시작 보기/끝 직전 보기**로 실제 프레임을 확인합니다. 타임라인의 보라색 손잡이와 우클릭 메뉴도 이때는 삭제가 아니라 쇼츠 소스 경계로 동작합니다.
2. 영상 위 선택 사각형을 이동하거나 8방향 손잡이로 크기를 바꿉니다. 자유 비율·9:16·1:1·원본 전체를 고를 수 있고 X/Y/너비/높이는 원본 픽셀 단위로 직접 입력하거나 방향키 1px, Shift+방향키 10px로 조절할 수 있습니다. 우측 설정 창이 화면 선택을 가리면 머리글의 **−**를 눌러 접으세요. 좌측 상단의 작은 **☰**만 남아 우측 영역을 그대로 드래그할 수 있고, 다시 누르면 입력값을 잃지 않고 펼쳐집니다.
3. **추가만** 또는 **추가하고 쇼츠에서 배치**를 누릅니다. 구간이 여러 본편 컷을 통과하면 컷 경계마다 독립 영상으로 나뉩니다. 자막·사진·음성은 암묵적으로 복사하지 않습니다. 필요한 원본 음성은 쇼츠 영상 선택 뒤 **원본 음성 에셋 추가**를 눌러 명시적으로 넣습니다.
4. 쇼츠에서는 1080×1920 검은 캔버스 위 영상 테두리를 직접 드래그하고 8방향 손잡이 또는 X/Y/너비/높이 값으로 픽셀 단위 배치합니다. 최초 영상을 포함해 모든 영상은 같은 독립 에셋이므로 각각 선택·삭제·표시 전환·앞뒤 이동할 수 있습니다. 영상이 없는 시간도 유효한 검은 캔버스입니다.
5. 한 화면에 영상을 더 합치려면 **+ 영상 추가**를 누릅니다. 본편으로 돌아가 같은 현재 원본 안의 다른 시간 구간과 crop을 고른 뒤 현재 재생 시각에 던집니다. 한 시각에는 영상 최대 9개를 겹칠 수 있습니다. 각 영상 구간은 별도 로컬 미리보기 파일로 준비되며, 4K 원본은 임의의 좁은 crop을 나중에 확대해도 디테일을 잃지 않도록 UHD 범위까지 보존합니다. 실행 취소·다시 실행과 영상에서 분리한 원본 음성도 같은 작은 파일을 재사용합니다.
6. 1–24px의 의도치 않은 외곽 틈이나 영상 사이 seam이 있으면 프레이밍 검사기의 **밀대로 모두 밀기**를 사용합니다. 현재 재생 시각의 관련 영상 사각형만 검출 결과에 맞춰 확장하고 원본 crop은 바꾸지 않습니다. 큰 여백은 의도한 배치로 유지됩니다.
7. 쇼츠 작업 공간에서 **쇼츠 내보내기**를 누릅니다. 영상은 뒤에서 앞으로 합성되고 그 위에 사진과 자막이 그려집니다. 출력 전 최종 합성 화면의 미세 틈도 시간 구간별로 다시 검사합니다.

각 영상과 원본 음성 에셋은 본편 lineage와 처음 고른 원본 범위를 보존하므로 추가 로컬 구간 준비 범위에는 계속 포함되지만, 쇼츠에서 한 시간·자막·사진·음성·프레이밍 변경은 본편을 바꾸지 않습니다. 현재 버전의 여러 영상은 한 프로젝트에 연결된 **같은 로컬 원본** 안에서만 가져옵니다. 서로 다른 파일을 한 화면에 연결하는 기능은 아직 제공하지 않습니다. 별도 파일명에는 `-shorts`가 붙습니다.

미리보기와 최종 출력은 같은 원본 crop·1080×1920 캔버스 사각형과 자동 품질 정책을 사용합니다. 이 정책은 crop의 가로·세로 실제 확대율을 매 프레임 각각 계산하고, 확대되는 축에 separable Lanczos2 재구성을 적용합니다. 1배에서는 불필요한 선명화를 끄고 확대할 때만 노이즈 제한 디테일 보정의 강도를 동적으로 올립니다. 처리 결과는 주변 색 범위 안으로 제한해 과도한 테두리와 빛 번짐을 줄이며, 쇼츠 출력에는 본편보다 높은 비트레이트 정책도 적용합니다. 첫 적응형 프레임을 합성하기 전에 WebGL2를 쓸 수 없으면 그 출력은 Canvas2D 고품질 보간으로 고정합니다. 적응형 프레임을 한 장이라도 합성한 뒤 context loss·메모리 부족 같은 오류가 나면 부분 파일을 중단하고, **호환 렌더러로 처음부터 다시 처리**를 표시한 뒤 같은 내보내기를 Canvas2D로 딱 한 번 재시작합니다. 따라서 완성 파일 안에는 두 렌더러의 프레임이 섞이지 않습니다. 다만 1920×1080 영상에서 화면을 가득 채우는 9:16 crop은 실제 원본 약 608×1080px를 1080×1920px로 키우는 작업입니다. 보정이 원본에 없던 실제 디테일까지 만들 수는 없으므로 1.25배를 넘는 확대나 가로·세로 비율 변형은 픽셀 검사기에 경고합니다. 가능하면 더 높은 해상도의 정식 원본을 연결하거나 crop을 덜 확대하세요.

## 영상 내보내기

**영상 내보내기**에서 폴더를 선택하면 다음 파일을 같은 폴더에 저장합니다.

- 편집 영상 MP4 또는 WebM
- 무결성 SHA-256이 포함된 `.kirinuki-session.json` 세션 복원 파일(이미지 에셋 포함, 원본 VOD 본문 제외)
- 자막이 있을 때 SRT

같은 이름이 이미 있으면 덮어쓰지 않도록 새 이름을 제안합니다. 브라우저가 폴더 저장 API를 지원하지 않으면 파일별 다운로드로 폴백할 수 있습니다.

폴더 저장은 완성 파일을 다시 열어 영상·필요 음성 트랙, 해상도, 길이와 안정된 파일 크기를 검증하고, 세션 복원 JSON과 SRT도 전체 바이트 SHA-256 및 복원 가능 여부를 확인합니다. 확인창이 열린 뒤 저장 파일이 바뀌지 않았는지 다시 검증한 경우에만 **세션 완료·로컬 재료 삭제**를 실행합니다. 제품의 기본 운영 단위는 **1세션 1편집**입니다. 정리를 고르면 정확한 `projectId + consumer + materialization + source` identity로 이 세션 전용 CHZZK·YouTube·SOOP 준비 조각·compact 영상·영수증을 먼저 격리·삭제하고, 같은 IndexedDB transaction에서 현재 프로젝트·임시저장·쇼츠 영상 캐시·이미지 에셋·파일 핸들을 모두 지웁니다. 다른 프로젝트와 사용자가 직접 고른 원본 파일은 건드리지 않습니다. 완료 뒤에는 자동저장과 편집을 끝내며, 다시 작업하려면 보존된 `.kirinuki-session.json`을 가져옵니다. 중간에 전원이 꺼지면 삭제 의도·완료 표식과 정확한 원본 binding을 대조해 이미 지운 VOD를 오래된 URL로 다시 연결하지 않습니다. companion 재시작 때는 정확한 상태 root 바로 아래 `.purge-quarantine/consumer-<hash>-<nonce>` 형태의 실제 디렉터리만 검증 후 회수하며, 이상 이름·심볼릭 링크·경계 변경이 하나라도 보이면 현재 consumer scope나 다른 파일을 대신 지우지 않고 VOD 기능을 fail-closed합니다. 일반 브라우저 다운로드 폴백, 검증 실패 또는 같은 프로젝트의 편집기 탭이 둘 이상 열린 경우에는 캐시를 삭제하지 않습니다.

내보내기 전 확인:

- 모든 컷의 시작과 끝
- 내부 삭제 뒤 자막·에셋·음성 싱크
- 모든 AudSeg 자리표시자 제거 또는 실제 문구로 교체
- Whisper 검수 경고
- 화자 색과 자막 위치
- 음소거 경계
- 투명 에셋
- 이번 VOD의 최신 권한 근거와 허용 범위
- 음원·출연자·제3자 권리와 게시 플랫폼 정책

## 권한과 네트워크

정상 실행은 최소 companion 밖의 광범위한 브라우저 권한을 사용하거나 다른 열린
페이지를 읽지 않습니다.
브라우저 연결은 exact `http://127.0.0.1:4320` Origin에서 다음 loopback 경계로
한정됩니다.

- VOD/caption gateway: `http://127.0.0.1:4319`
- 선택적 Whisper private engine: `127.0.0.1:4318` (gateway만 접근)
- AudSeg: 브라우저 내부 처리
- 원격 자막 호스트 권한: 없음
- 자막 API 키 입력: 없음
- 원본 업로드: 없음

Whisper session token은 companion 프로세스와 현재 편집기 탭 메모리에만 있으며 프로젝트, 임시저장, 브라우저 저장소, 로그에 기록하지 않습니다.

## 문제 해결

### localhost 시작 화면이 열리지 않음

`kirinuki status`에서 localhost studio가 ready인지 확인하고, 아니면
`kirinuki stop` 후 `kirinuki start`를 실행합니다. 주소는 정확히
`http://127.0.0.1:4320`이어야 하며 임의의 다른 포트나 `file://`로 HTML을
직접 열지 않습니다. 4320을 다른 프로세스가 점유하면 Kirinuki는 그 프로세스를
강제로 종료하지 않고 fail-closed합니다.

### VOD 편집 영상을 준비하지 못함

먼저 `kirinuki status`에서 4319 gateway가 ready인지 확인합니다. CHZZK·SOOP은
선택된 공개 HLS의 플레이어 시간축과 조각 구조를, YouTube는 선택된 직접 입력의
0초 원점과 길이를 증명할 수 있을 때만 준비합니다. 실패하면 해당 영상이 완료 VOD인지, 로그인·
성인·지역·DRM 제한 없이 공개되는지 확인하세요. 쿠키나 로그인 우회로 재시도하지
않으며, 권한 있는 로컬 원본이 있으면 **내 파일 직접 연결**을 사용합니다.

### 내 파일 직접 연결 뒤 싱크가 맞지 않음

**페이지 시각 ↔ 로컬 원본 정렬** 오프셋을 확인합니다. 다른 인코딩 파일을 연결했다면 파일의 시작 지점과 재생 길이가 같은지 먼저 확인합니다.

### Whisper 자동 연결이 되지 않음

다음 순서로 확인합니다.

1. `npm run caption-stack:status`에서 실행 상태를 확인합니다.
2. 꺼져 있으면 `npm run caption-stack:start`를 실행합니다.
3. 이전 브라우저 확장 Origin이나 다른 포트로 setup한 적이 있다면 현재 exact localhost Origin용으로 `npm run caption-stack:setup`을 다시 실행합니다.
4. 원하는 profile과 실행 중인 모델이 다르면 stack을 정상 종료하고 해당 profile로 setup·start합니다.
5. 편집기의 **Whisper** 탭에서 **이 PC의 Whisper 자동 연결**을 다시 누릅니다.

`caption-stack:setup`이 남기는 `kirinuki-whisper-connection.json`은 내부 진단용 산출물입니다. 정상 편집 흐름은 이 파일을 열거나 읽지 않으며, 사용자에게 JSON·Whisper 실행 파일·모델 파일·경로를 고르게 하지 않습니다.

### Whisper 엔진이 꺼졌다고 나옴

```bash
npm run caption-stack:status
npm run caption-stack:start
```

설정이 없으면 `npm run caption-stack:setup`을 먼저 실행합니다. 4318 또는 4319 포트를 다른 프로세스가 쓰고 있으면 그 프로세스를 정상 종료한 뒤 다시 시작합니다. 시작한 뒤 편집기의 **Whisper** 탭에서 **이 PC의 Whisper 자동 연결**을 다시 누르세요.

### Whisper가 영어처럼 인식함

`.en` 모델이 아니라 다국어 모델인지 확인합니다. 기본 프로필로 복구:

```bash
npm run caption-stack:setup -- --profile draft
```

### AudSeg cue가 너무 많음

음악과 효과음도 활동으로 감지되는 것이 정상입니다. 필요 없는 cue를 지우거나 Whisper를 사용하세요. AudSeg 결과를 전사 결과처럼 간주하지 마세요.

### AudSeg cue에 실제 글이 없음

정상 동작입니다. AudSeg는 타이밍만 만들고 모든 텍스트는 사람이 채웁니다.

### 자막 시작·끝을 바꿀 수 없음

자막 블록 본문이 아니라 양끝 손잡이를 잡았는지 확인합니다. 정밀 값은 선택 자막 패널의 시작·끝 입력에 직접 넣을 수 있습니다.

### 내보내기가 느리거나 메모리가 부족함

다른 무거운 앱을 닫고 컷을 작은 프로젝트로 나눕니다. 자동 준비한 CHZZK·YouTube·SOOP VOD는 compact 로컬 편집 영상만 사용하고, 수동 연결 영상도 파일 전체를 메모리에 올리지는 않습니다. 다만 인코딩 자체에는 CPU·GPU와 프레임 버퍼가 필요합니다.

### 저장된 원본을 다시 열 수 없음

브라우저의 파일 권한이 만료된 것입니다. 프로젝트는 그대로 둔 채 **내 파일 직접 연결**에서 같은 파일을 다시 고릅니다. 오래된 복구본부터 강제로 불러오지 마세요.

## 알려진 제한

- Whisper와 AudSeg 결과는 모두 초안이며 사람 검수가 필요합니다.
- Whisper는 모델에 관계없이 고유명사, 겹친 음성, 빠른 말과 잡음에서 틀릴 수 있으며 Tiny에서 더 두드러질 수 있습니다.
- AudSeg는 발화와 음악·효과음을 구분하지 않고 텍스트나 화자를 만들지 않습니다.
- Whisper 자막 자동 생성은 한 번에 활성 컷 16개 상한이 있습니다. AudSeg에는 활성 컷 개수 상한이 없지만 컷당 30분·Float32 PCM 128MiB와 실행당 새 cue 10,000개 상한이 있습니다.
- 원본 디코더와 브라우저 인코더 지원은 코덱과 환경에 따라 다릅니다.
- 게시 가능 여부, 음원, 수익화와 제3자 권리는 자동 승인하지 않습니다.

## 최종 확인

- [ ] 사용자 확정 컷과 순서가 맞음
- [ ] 이번 원본 회차와 열기 목적에 맞춰 권리·책임 확인을 직접 입력함
- [ ] 연결한 로컬 원본과 오프셋이 맞음
- [ ] 선택한 초벌 방식의 의미를 이해함
- [ ] AudSeg 자리표시자를 모두 사람이 확인함
- [ ] Whisper 오인식과 검수 경고를 원음으로 확인함
- [ ] 모든 자막의 시작·끝이 의도한 발화와 읽기 호흡에 맞음
- [ ] 문장 끝 불필요한 마침표를 제거함
- [ ] 화자 색과 화면 위치가 일관됨
- [ ] 이미지 투명도와 자막·에셋 타이밍이 맞음
- [ ] 짧은 음소거 경계를 반복 재생해 확인함
- [ ] 내보낸 영상·JSON·SRT를 실제로 열어 봄
- [ ] 공개 직전에 방송인·음원·제3자·게시 플랫폼의 최신 조건을 사람이 다시 확인함
